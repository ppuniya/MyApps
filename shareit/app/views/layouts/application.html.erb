<!DOCTYPE html>
<html>
<head>
  <title>Shareit</title>
  <!--[if lt IE 9]>
  	<script src="http://html5shim.googlecode.com/svn/trunk/html5.js" type="text/javascript"></script>
  <![endif]-->
  <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
  <%= csrf_meta_tags %>
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
</head>
<body onload="setup_board()">

<div id="wrap">
<nav class="navbar navbar-default" role="navigation">
	<div class="container">
		<div class="navbar-header">
			<button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target=".nav-collapse">
				<span class="sr-only">Navigation</span>
				<span class="icon-bar"></span>
				<span class="icon-bar"></span>
				<span class="icon-bar"></span>
			</button>
			<a class="navbar-brand" href=<%= root_url %>><code class="lead">ShareIt</code></a>
			<p class="navbar-text">
				<% if current_user %>
					Logged in as <%= current_user.fname %>.
				<% else %>
					Not logged in.
				<% end %>
			</p>
		</div>
		<div class="collapse navbar-collapse nav-collapse">
			<ul class="nav navbar-nav navbar-right">
				<% if content_for? :menu %>
					<%= yield :menu %>
				<% end %>
			</ul>
		</div>
	</div>
</nav>

<div class="container">
	<% flash.each do |name, msg| %>
		<div class="alert alert-<%= name == "notice" ? "success" : "danger" %>">
			<%= msg %>
		</div>
	<% end %>
	
	<%= yield %>
</div>
</div>

<div id="footer">
	<div class="container">
	  <p class="text-muted text-center credit"><span class="glyphicon glyphicon-copyright-mark" aria-hidden="true"></span>
	  Shareit is developed by Prashant Puniya.</p>
	</div>
</div>

</body>
</html>
